export const HEADER_W = 200
export const RULER_H = 28
/** Tall enough for the header's three rows: name, pan+volume, toggles. */
export const LANE_H = 92
/** Compact mode: just the track name, so many tracks fit on screen. */
export const COMPACT_LANE_H = 30
export const AUTO_H = 44
/** One sub-lane of an expanded take stack (state/takeLanesUi). */
export const TAKE_LANE_H = 34
/** The "drop files here" bar that always closes the track list. */
export const DROP_BAR_H = 46

export const MIN_PX_PER_BEAT = 4
export const MAX_PX_PER_BEAT = 200
